import { StyleSheet, View } from 'react-native'
import React from 'react'

const Card = ({ children }) => {
    return (
        <View >
            {
                children
            }
        </View>
    )
}

export default Card

const styles = StyleSheet.create({

})